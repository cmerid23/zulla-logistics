Drop binary PWA icons here:

  icon-192.png         (192x192)
  icon-512.png         (512x512, also used as the maskable variant)
  apple-touch-icon.png (180x180 recommended)

These files are referenced by manifest.json and index.html. The scaffold
ships without binaries so the repo stays small — generate them from the
brand mark before the first deploy.
